# The URL the site will be built for
base_url = "https://eiasamoah.me"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true
math = true

[extra]
# Put all your custom variables here
url = "https://github.com"
# A line to display underneath the main title
subtitle = "Example subtitle"

# Text to display in the footer of the page
copyright = "Copyright authors year"

# Your Google Analytics ID
analytics = ""

# See below
katex_enabled = true
katex.enabled = true
katex.auto_render = true

# See below
instantpage_enable = false
toc = true

[extra.favicon]
favicon_32x32 = "static/icons/favicon/favicon-32x32.png"